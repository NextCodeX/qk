tableSql  = "
CREATE TABLE `userinfo` (
    `uid` INTEGER PRIMARY KEY AUTOINCREMENT,
    `username` VARCHAR(64) NULL,
    `departname` VARCHAR(64) NULL,
    `created` DATE NULL
);
"

db = sqlite("foo.db")
// db.exec(tableSql)
// db.exec("insert into userinfo(uid, username) values(114, 'everything')")
rows = db.getRows("select * from userinfo")
forv row : rows {
    println(`id: ${row.uid}, name: ${row.username} (in dynamic string)`)
}
