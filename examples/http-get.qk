/*
resp = httpGet("http://localhost:12345/hello?from=qk", {check:true})
printf("status: %v|%v \n", resp.status(), resp.statusCode())
println(resp.string())
// obj = resp.json()
// println("ret is", obj.ret, obj.msg)*/


url = "https://api.uomg.com/api/rand.img3?sort=胖次猫&format=json"
i = 1
for i < 4 {
    r = httpGet(url)
    println("===============================")
    foreach k, v : r.headers() {
        println(k, "=", v)
    }
    println("===============================")
    println(r.string(), r.json().imgurl)
    // download img
    r = httpGet(r.json().imgurl)
    r.save(i+".jpg")

    i++
}
