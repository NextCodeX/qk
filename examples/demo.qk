dir = "/home/xp/Pictures/wallpaper/"

fs = fscan(dir)
count = 1
forv abspath : fs {
    name = abspath[abspath.lastIndex("/")+1:]
    suffix = name[name.index("."):]
    cmd(`mv ${abspath} ${dir}/${count}${suffix}`)
    count++
}